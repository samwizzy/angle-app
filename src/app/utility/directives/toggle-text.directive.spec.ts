import { ToggleTextDirective } from './toggle-text.directive';

describe('ToggleTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleTextDirective();
    expect(directive).toBeTruthy();
  });
});
